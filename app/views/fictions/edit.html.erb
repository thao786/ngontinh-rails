<h1>Editing fiction</h1>

<%= link_to 'Destroy', fiction_path(@fiction),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>

<%= link_to 'Add Chapter', new_fiction_chapter_path(@fiction) %>

<%= form_for :fiction, url: fiction_path(@fiction), method: :patch do |f| %>

<% if @fiction.errors.any? %>
<div id="error_explanation">
    <h2>
        <%= pluralize(@fiction.errors.count, "error") %> prohibited
        this article from being saved:
    </h2>
    <ul>
        <% @fiction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
</div>
<% end %>

    <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

    <p>
    <%= f.label :alternate %><br>
    <%= f.text_field :alternate %>
  </p>

    <p>
    <%= f.label :author %><br>
    <%= f.text_field :author %>
  </p>

    <p>
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </p>

    <p>
    <%= f.label :language %><br>
    <%= f.text_field :language %>
  </p>
    <p>
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </p>

    <p>
    <%= f.label :editor %><br>
    <%= f.text_field :editor %>
  </p>

    <p>
    <%= f.label :translator %><br>
    <%= f.text_field :translator %>
  </p>


<p>
    <%= f.submit %>
</p>

<% end %>

<%= link_to 'Back', fictions_path %>